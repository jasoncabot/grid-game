{
    "name": "chain-reaction",
    "description": "Chain Reaction — a pipe puzzle game.",
    "version": "2.0.0",
    "scripts": {
        "dev": "vite --config vite/config.dev.mjs",
        "build": "vite build --config vite/config.prod.mjs"
    },
    "devDependencies": {
        "terser": "^5.31.0",
        "typescript": "^5.4.5",
        "vite": "^5.3.1"
    }
}
